# 팀편성 기능 4건 추가

대상: `src/App.jsx` (Step 1 팀 편성 영역)
저장 스키마(Supabase) 변경 없음 — 모두 클라이언트 로직.

---

## 1. 수동 일부 배치 → 나머지 자동 채우기

**추가**: `fillRemainingAuto()` 함수 + 수동 모드 "미배치 인원" 영역에 `🤖 나머지 자동 배치` 버튼.

**동작**:
- 수동으로 일부 배치한 상태에서 버튼 클릭 → 미배치 인원만 빈 슬롯에 자동 배치
- 동반 게스트가 이미 배치된 파트너 카트가 있으면 그곳에 합류 (슬롯 여유 시)
- 큰 동반 묶음은 빈 슬롯 많은 카트로, 단일 인원은 현재 평균이 낮은 카트에 배치(밸런스)
- 슬롯 부족 시 카트 자동 추가

---

## 2. 페어 히스토리 표시 (같은 카트로 라운딩한 횟수)

**추가**:
- `pairCount` useMemo: 전체 과거 라운드의 카트 페어 횟수 집계 (정회원만, 게스트 제외)
- **카트 내 표시**: 각 정회원 옆에 `🔁N` — 같은 카트의 다른 정회원들과의 누적 합계
- **매트릭스 패널**: 카트 표시 아래 접이식 `📊 페어 히스토리 매트릭스` — 정회원×정회원 표, 값이 클수록 진한 호박색

자동/수동 모드 모두 적용.

---

## 3. 자동편성 모드 선택 (확장 가능)

**구조**: `AUTO_MODES` 상수 배열로 정의. 향후 모드 추가는 한 줄만 추가하면 됨.

```js
const AUTO_MODES = [
  { id: "cart_avg",     label: "카트별 평균 밸런스",   desc: "..." },
  { id: "seat_balance", label: "앞·뒷자리 밸런스",      desc: "..." },
  { id: "pair_minimize",label: "페어 히스토리 최소화",  desc: "..." },
];
```

**UI**: 자동 편성 버튼 위에 모드 select + 설명 한 줄.

**모드별 동작**:
- `cart_avg`: 기존 스네이크 드래프트 — 카트별 평균 핸디를 맞춤
- `seat_balance`: 스네이크 드래프트로 카트 구성 후, 각 4명 카트의 좌석을 (1·2번 avg ≈ 3·4번 avg) 가 되도록 재배치. 3가지 페어링 중 차이 최소 선택. 동반 게스트가 있는 카트는 동반자끼리 같은 그룹(앞 또는 뒤)에 강제 묶음
- `pair_minimize`: 동반 묶음을 빈 카트에 먼저 배치 → 단일 인원은 현재 카트 멤버들과의 과거 페어 횟수 합이 가장 낮은 카트에 그리디 배치. 카트 평균은 고려하지 않음 (사용자 명시 의도)

**부가 표시**: 4명 카트면 표시 영역에 `앞 X.X / 뒤 Y.Y` 와 좌석 번호(1/2/3/4), 2/3번 사이 점선 — 모드 무관하게 항상 보임

---

## 4. 게스트 동반자 추후 편집

**변경**: 게스트 리스트 행에 표시되던 `→ 홍길동 동반` 텍스트를 select로 교체.
- 옵션: "동반 없음" + 현재 참석 정회원 이름들
- 추가 후 언제든 동반자 변경/해제 가능
- 변경 후 카트 재편성은 자동 트리거 안 함 (수술적 변경 원칙) — 사용자가 자동 편성 버튼을 다시 누르면 새 동반자 정보로 재편성

---

## 알고리즘 가정

- 카트 정원: 4명 고정 (기존 동일)
- 게스트는 페어 히스토리 카운트 대상 아님 (과거 데이터가 없거나 정회원과 비교 의미 약함)
- 페어 매트릭스/카트 내 페어 카운트는 정회원만
- `pair_minimize` 는 그리디 (완전탐색/시뮬레이티드 어닐링 미적용) — 사람 수가 보통 12명 이하라 충분

---

## 검증

- `npm run build` 통과
- 4가지 기능 모두 Step 1 화면 안에서 완결 — 다른 화면/스코어 입력/저장 로직 변경 없음
- 카트 데이터 구조는 기존 그대로 (`cartTeams: [[id, id, id, id], ...]`) — 좌석 순서(앞/뒤)는 배열 순서로 표현, Supabase `cart_teams` 행 순서로 보존됨
